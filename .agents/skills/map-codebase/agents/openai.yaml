interface:
  display_name: "Map Codebase"
  short_description: "Map repository structure and change surfaces."
  default_prompt: "Use $map-codebase to produce a read-only repository map for the approved planning item."
