# Planning evaluation fixtures

These JSON files contain sanitized, committed recorded results only. They do not contain live model output, credentials, repository secrets, or private planning artifacts. `scripts/evaluate_planning.py` reads them deterministically and requires 100% recall for required literal assertions and zero forbidden matches.
